/**
 * Los nombres de los iconos.
 *
 * El dibujo lo hace `components/Icon.svelte`: los iconos son una fuente
 * tipografica que se carga del CDN (ver `shared/icons.ts`), así que no hay
 * catálogo ni trazados, solo nombres.
 */
export { ICON_NAMES } from "@shared/iconNames";
export { DEFAULT_TABLE_ICON } from "@shared/icons";
