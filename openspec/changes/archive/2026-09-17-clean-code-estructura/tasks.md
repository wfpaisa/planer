Cada grupo es una fase independiente (ver `design.md` - Migration Plan): se puede implementar, verificar y commitear sola. Verificación mínima después de cada grupo: `bun run typecheck`. Se agrega `bun run lint` y/o `bun run harness` donde aplica. `AGENTS.md` y `docs/COMO-FUNCIONA.md` se actualizan en el mismo grupo que mueve el archivo que nombran, no al final.

## 1. `server/aiPage.ts` → `server/aiPage/`

- [x] 1.1 Crear `server/aiPage/prompts.ts`: mover `TOOL_GUIDE`, `PLAN_MODE_GUIDE`, `UNNAMED_PAGE`, `USE_SYSTEM`, `FIX_SYSTEM_TAIL`, `systemPrompt()`.
- [x] 1.2 Crear `server/aiPage/tools.ts`: mover `FIELD_SCHEMA`, `BLOCK_REF`, `BLOCK_NAME`, `BLOCK_SOURCES`, `TOOLS`, `PLAN_MODE_WRITE_TOOLS`, `CERRAR_PLAN_TOOL`, `toolsFor()`.
- [x] 1.3 Crear `server/aiPage/context.ts`: mover `sourcesFor`, `pickedSection`, `filesSection`, `weigh`, `withSources`, `missingFile`, `findTable`, `titleFromHtml`, `unescapeHtml`.
- [x] 1.4 Crear `server/aiPage/fields.ts`: mover `FIELD_TYPES`, `RETYPE_TYPES`, `normalizeFieldDefs`, `planFill`, `accessDirection`.
- [x] 1.5 Crear `server/aiPage/toolRuntime.ts`: mover `runTool()` y `hold()`. Dentro de `runTool`, nombrar como función propia cada `case` largo del switch (p. ej. `runCrearTabla`, `runEscribirPagina`, `runReemplazarBloque`...) para que el switch quede como índice de una línea por herramienta; el cuerpo de cada handler es el mismo código que hoy, sin cambios de lógica.
- [x] 1.6 Dejar en `server/aiPage.ts` (o renombrarlo a `server/aiPage/index.ts`, decidir según qué import quede más limpio en `routes.ts`) solo el orquestador: `appPages`, `appTables`, `runPageRequest`, `explainPageUse`, `fixPage`, `writePageDoc`, `priorTurns`, `stepBefore`, `MAX_HISTORY_*`, importando de los módulos de arriba.
- [x] 1.7 Actualizar el único import externo (`server/routes.ts`) y cualquier otro que aparezca al correr `bun run typecheck`.
- [x] 1.8 Actualizar las rutas mencionadas en `AGENTS.md` (busca `server/aiPage.ts`) y en `docs/COMO-FUNCIONA.md` (busca `TOOL_GUIDE`, `server/aiPage.ts`).
- [x] 1.9 `bun run typecheck` y `bun run lint` limpios. Probar en el navegador: pedirle a la IA un cambio simple en una página y confirmar que responde igual que antes.

## 2. `server/ai.ts` y compañía → `server/ai/`

- [x] 2.1 Mover `ai.ts`, `aiChats.ts`, `aiDebug.ts`, `aiFiles.ts`, `aiModels.ts`, `aiRuns.ts` a `server/ai/` (mover tal cual, sin dividir contenido).
- [x] 2.2 Mover la carpeta `server/aiPage/` (creada en el grupo 1) dentro de `server/ai/aiPage/`, o evaluar si conviene que sus archivos queden sueltos directamente en `server/ai/` — decidir según cuál lectura es más clara antes de mover. Decisión: se mantiene como subcarpeta (`server/ai/aiPage/`) en vez de sueltos: con los 6 archivos generales de `ai/` más los 6 de `aiPage/`, 12 archivos sueltos en una sola carpeta plana sería peor lectura que la subcarpeta que ya distingue "escribir una página" del resto.
- [x] 2.3 Actualizar todos los imports que rompan (`server/routes.ts` es el principal).
- [x] 2.4 Actualizar rutas mencionadas en `AGENTS.md` y `docs/COMO-FUNCIONA.md`.
- [x] 2.5 `bun run typecheck` y `bun run lint` limpios.

## 3. `server/html*.ts` → `server/html/`

- [x] 3.1 Mover `htmlAudit.ts`, `htmlBlocks.ts`, `htmlBridge.ts`, `htmlCharts.ts`, `htmlDocs.ts`, `htmlProbe.ts` a `server/html/` (mover tal cual).
- [x] 3.2 Actualizar todos los imports que rompan.
- [x] 3.3 Actualizar rutas en `AGENTS.md` y `docs/COMO-FUNCIONA.md` (busca `htmlAudit.ts`, `htmlBlocks.ts`, `KNOWN_VARS`).
- [x] 3.4 `bun run typecheck`, `bun run lint` y **`bun run harness`** limpios (el harness depende de rutas relacionadas con estilos/contrato).

## 4. `server/page*.ts` → `server/page/`

- [x] 4.1 Mover `pageAssets.ts`, `pageConvert.ts`, `pageData.ts`, `pageStyles.ts` a `server/page/` (mover tal cual).
- [x] 4.2 Actualizar todos los imports que rompan.
- [x] 4.3 Actualizar rutas en `AGENTS.md` (busca `server/pageData.ts`, `server/pageStyles.ts`) y `docs/COMO-FUNCIONA.md`.
- [x] 4.4 `bun run typecheck`, `bun run lint` y `bun run harness` limpios (`pageStyles.ts` sirve `/plane/estilos.css`). También corregido `new URL("../web/src/styles/...")` en `pageStyles.ts`, que apuntaba relativo al archivo y necesitaba un `../` más tras el movimiento.

## 5. `DataGrid.svelte`: exportación y visibilidad de columnas a `lib/`

- [x] 5.1 Crear `web/src/lib/dataGridExport.ts` con `exportTable`, `exportAll`, `exportSelected` (y sus tipos/imports asociados).
- [x] 5.2 Crear `web/src/lib/dataGridColumns.ts` con `toggleHidden`, `toggleSystemVisible`, `isSystem`, `systemFieldDef`.
- [x] 5.3 `DataGrid.svelte` importa y llama a esas funciones en vez de definirlas. El `exportTable` local se quitó del todo (quedó sin uso: `exportAll`/`exportSelected` ahora llaman al de la lib directamente); `toggleHidden`/`toggleSystemVisible` quedaron como wrappers de una línea con la misma firma que antes, así que el template no cambió.
- [x] 5.4 `bun run typecheck` y `bun run lint` limpios. Probado en el navegador: descargar CSV (toda la tabla y solo seleccionadas), copiar CSV al portapapeles, ocultar/mostrar una columna de negocio y una del sistema — todo funciona igual que antes, sin errores nuevos en consola.

## 6. `ImportModal.svelte`: pasos de asistente

- [x] 6.1 Crear `web/src/lib/importWizard.svelte.ts` con el estado compartido del asistente (columnas mapeadas, reglas por columna, filas convertidas, paso actual) que hoy vive como `$state` local de `ImportModal.svelte`. Es una clase (`ImportWizard`) y no el singleton de módulo que usan `aiRun.svelte.ts`/`pendingImport.svelte.ts`: el estado es de un diálogo que nace y muere, no de la aplicación. La tabla llega como función (`new ImportWizard(() => table)`) para que siga leyéndose la prop viva.
- [x] 6.2 Extraer el paso de lectura/parseo de archivo (`readFile` y alrededores) a un subcomponente o a `web/src/lib/importModal.ts` si no necesita markup propio. `readFile` se fue con el estado al `ImportWizard` (6.1) y la pantalla que lo usa —pestañas archivo/texto, zona de arrastre, recuento y aviso— a `components/import/ImportContent.svelte`, con su CSS.
- [x] 6.3 Extraer el paso de mapeo de columnas a `components/import/` como subcomponente, usando `ImportColumnHead.svelte` ya existente como pieza. Es `components/import/ImportGrid.svelte`: el emparejamiento no tiene pantalla propia —se hace en el encabezado de cada columna de la grilla— así que la grilla entera (encabezados con `ImportColumnHead`, filas y su CSS) es la pieza.
- [x] 6.4 Extraer el paso de previsualización/confirmación (`statusOf`, `buildBody`, `save`, `guardar`) a subcomponente o módulo, lo que separe mejor markup de lógica sin fragmentar en exceso. A `web/src/lib/importSave.ts`: `rowStatus`, `buildBody`, `createColumns`, `resolvePlans`, `buildRequests`, `writeBatches`, `peopleLeftover`, `peopleSummary`, `rowsSummary`. `save()` se queda en el componente como la cadena de los cinco pasos (~110 líneas en vez de ~300) porque es quien enciende el reloj, guarda el parte y llama a `onDone` en el mismo orden que antes; sacarlo entero habría obligado a devolver cada asignación de estado por separado para no cambiar cuándo ocurre.
- [x] 6.5 `ImportModal.svelte` queda como el contenedor del asistente: wiring del estado de `importWizard.svelte.ts` con cada paso. De 2330 a 1549 líneas.
- [x] 6.6 `bun run typecheck` y `bun run lint` limpios. Probar en el navegador: importar un CSV completo, de subida a guardado, incluyendo un caso con filas que fallan. Probado sobre la tabla `Sucursales` con un CSV de 4 filas, una con la latitud ilegible: se leyó el archivo (separador adivinado y paso solo a la previsualización), la fila mala salió marcada en rojo con su motivo, apagar y encender una columna desde el ojo del encabezado cambió el recuento y las celdas, el lote rechazado por la base se contó y se dijo, y al guardar con "continuar" marcado entraron 3 filas y quedó 1 fuera, con el parte en el diálogo y la tabla recargada. Las filas de prueba se borraron después.

## 7. `AiPanel.svelte`: extraer el ciclo de vida de una petición en marcha

- [x] 7.1 Crear un módulo `.svelte.ts` (nombre a definir según lo que ya usa `aiRun.svelte.ts`/`aiConversation.svelte.ts`) que contenga `listen()` completa: abrir el stream, acumular progreso, aterrizar el resultado (`land`). Es `web/src/lib/aiListen.ts`, **sin** el sufijo `.svelte`: no declara ni un `$state` —lo que se ve vive en `aiRun.svelte.ts` y `aiConversation.svelte.ts`, y esto solo los escribe—, así que el sufijo habría prometido reactividad que el archivo no tiene. Se lleva también `seen`/`seenKey`/`showProgress` y el reloj del lienzo (`refreshCanvas`), que solo usaba `listen`; el panel corta el reloj al desmontarse con `cancelCanvasRefresh()`.
- [x] 7.2 `AiPanel.svelte` llama a ese módulo en vez de definir `listen()` inline; `dispatch()` se queda en el componente. Lo que era del panel —a dónde mira la columna, qué filas están desplegadas, el marco escondido de las pruebas— viaja como funciones (`onListening`, `onLanded`, `onProbe`). De 2308 a 2090 líneas.
- [x] 7.3 `bun run typecheck` y `bun run lint` limpios. Probar en el navegador: una petición completa a la IA, cortarla a mitad, y que una pregunta (`preguntar`) se conteste bien. Las tres probadas sobre una página vacía de la app "Contar": la completa aterrizó con su respuesta, sus pasos y su contexto (y de paso se comprobó el reenganche por `/ia/seguir` tras recargar a mitad); la cortada aterrizó "Petición detenida." con el proceso desplegado, como corresponde a una sin terminar; y la pregunta pintó su tarjeta con opciones, se contestó y la petición siguiente aplicó el cambio en el lienzo.

## 8. `demo/DemoGallery.svelte`: dividir por sección del catálogo

- [x] 8.1 Identificar las secciones del catálogo que ya lista `DESIGN.md` (botones, campos, checkbox/radio/switch, cards, chips, tags, menú, modal/drawer/toast, tabla...) y confirmar cuáles corresponden a bloques de markup ya delimitados en el archivo actual. El corte no hay que inventarlo: el archivo ya trae cinco `div.gallery-group` con id, y son los mismos cinco que `DemoSidebar.svelte` lista en `CATALOG` y cuyas fichas cuenta por `#<id> .gallery-item` — `demo-navegacion` (194 líneas), `demo-contenido` (573), `demo-graficas` (126), `demo-formulario` (419) y `demo-retro` (520). Las piezas sueltas que enumera `DESIGN.md` (botones, chips, tags, modales...) son fichas dentro de esos grupos, no un nivel de corte: una carpeta de 40 archivos de una ficha cada uno no diría de un vistazo dónde está qué.
- [x] 8.2 Extraer cada sección a un archivo hermano bajo `web/src/components/demo/` (nombre a definir, p. ej. `DemoButtons.svelte`, `DemoCards.svelte`...), moviendo el snippet/markup correspondiente tal cual. Son `DemoNavegacion` (211 líneas, 7 fichas), `DemoContenido` (703, 15), `DemoGraficas` (314, 5), `DemoFormularios` (429, 13) y `DemoAvisos` (523, 7) — 47 fichas, las mismas de antes. El marcado se movió tal cual: comparado carácter a carácter contra el de `HEAD`, solo cambian tres comas finales que Prettier quitó de `head("...", "...",)` al quedar el marcado menos indentado. Cada sección se lleva el script y el CSS que solo ella usaba (las gráficas sus canvas y sus figuras; Contenido el estado de la tabla de pedidos y sus celdas) y recibe `head` como `Snippet` por props.
- [x] 8.2b Dos cosas que el corte obligó a tocar, ninguna de comportamiento: la disposición compartida salió a `demo/gallery.css` (importada desde `DemoGallery.svelte`) porque el CSS con ámbito de Svelte no alcanza al marcado de otro archivo, y sus nombres son de la galería y de nadie más; y `source.ts` ahora lee las cinco secciones en vez de `DemoGallery.svelte?raw`, sin lo cual el botón "Ver el código" se habría quedado vacío en las 47 fichas. Comprobado con el propio parser fuera del navegador: 47 fichas, ninguna vacía, ninguna perdida, ningún título repetido. El CSS también se comparó regla a regla: 33 antes, 33 ahora, mismo conjunto.
- [x] 8.3 `DemoGallery.svelte` compone las secciones extraídas. De 2565 a 253 líneas: se queda con el armazón —el snippet `head()` con su botón, el modal del código y su estado, y el masonry por script— y las cinco secciones debajo.
- [x] 8.4 `bun run typecheck` y `bun run lint` limpios. Abrir `/demo` en el navegador y confirmar que se ve igual que antes. Comprobado con el panel a 1440px: las cinco secciones montan (la barra lateral cuenta 7/15/5/13/7 fichas, los mismos números que salen de los archivos), el masonry reparte en dos columnas y se reacomoda al filtrar la tabla, la tabla de pedidos filtra y ordena, las gráficas cambian de rango y se repintan al cambiar la paleta (el observador que se fue con ellas sigue avisando), el modal y el cajón se abren desde dentro de su sección, y el botón «Ver el código» saca el ejemplo de una ficha que ya vive en otro archivo. Sin un solo error en consola.
- [x] 8.5 Fuera del navegador, comparado contra `HEAD` para descartar cambios colados: el marcado es idéntico carácter a carácter salvo tres comas finales que Prettier quitó de `head("...", "...",)`; el CSS son las mismas 33 reglas, mismo conjunto; y el parser de `source.ts` saca las 47 fichas, ninguna vacía, ninguna perdida, ningún título repetido.

## 9. Reagrupar `web/src/components/` por dominio

- [x] 9.1 Confirmar con el usuario los nombres de carpeta antes de mover nada: `database/` (DataGrid + piezas de la base de datos) y `app/` (sidebar, publicar, roles, versiones) — ver `design.md`, decisión 2. Confirmados `database/` y `app/`, y confirmado también ampliar el alcance: el `design.md` nombraba 13 archivos, pero dejaba fuera piezas que solo sirven a esa misma pantalla (`ColumnHead` lo usan DataGrid e ImportGrid; `DrawerField` solo RowDrawer; `PasswordBlock` solo DataGrid). El criterio aplicado es el uso real, comprobado con un mapa de quién importa a quién, no el prefijo del nombre.
- [x] 9.2 Crear `web/src/components/database/` y mover `DataGrid.svelte`, `ColumnModal.svelte`, `RowDrawer.svelte`, `OrphanPanel.svelte`, `ImportModal.svelte`, `ImportStructureModal.svelte`, y la carpeta `import/` ya existente. Van 15 archivos y 2 carpetas: los seis de la lista más `TableSidebar`, `ColumnHead`, `FieldIcon`, `DrawerField`, `PersonColumnOffer`, `PasswordBlock`, `PasswordForm`, `PasswordNote` y `RolesModal`, y las carpetas `import/` y `cells/` (a `cells/` solo la usan DataGrid y RowDrawer). `RolesModal` y `PasswordForm` estaban en `app/` en el `design.md`: se quedan en `database/` porque cuelgan de DataGrid y dejarlas fuera partía la cadena `DataGrid → PasswordBlock → PasswordForm → PasswordNote` en dos carpetas.
- [x] 9.3 Crear `web/src/components/app/` y mover `AppSidebar.svelte`, `CreateAppModal.svelte`, `PublishPanel.svelte`, `RolesModal.svelte`, `PasswordForm.svelte`, `HistoryPanel.svelte`, `VersionRow.svelte`. Van 10: los cinco que quedan de la lista (sin `RolesModal` ni `PasswordForm`, ver 9.2) más `AppTopBar` —la otra mitad del marco, junto a `AppSidebar`—, `AppName` y `PublishButton` (solo los usa `AppTopBar`), `AppIcon` (la identidad de la aplicación) y `WatchingBadge` (solo `HistoryPanel` y `VersionRow`).
- [x] 9.4 Actualizar todos los imports que rompan en `web/src/`. 41 archivos reescritos por script, resolviendo cada ruta relativa contra su destino nuevo en vez de sustituir texto a ciegas. `typecheck` pasó a la primera. También se corrigió el comentario de `shared/importBatch.ts`, que nombraba `components/ImportModal.svelte`. `AGENTS.md` y `docs/COMO-FUNCIONA.md` no hacía falta tocarlos: de `components/` solo nombran `ui/` e `Icon.svelte`, y ninguno se movió.
- [x] 9.5 `bun run typecheck` y `bun run lint` limpios. Probar en el navegador: abrir la base de datos de una tabla, publicar una app, ver el histórico de versiones. Probado: la pantalla de la base de datos abre con su lista de tablas, su grilla y sus encabezados, el diálogo de Roles abre, y una fila abre su cajón con sus campos y sus celdas; el histórico de cambios lista sus versiones con el aviso de cambios sin publicar; y el panel de Publicar abre con su interruptor (se cerró sin guardar, para no cambiar el estado de publicación de la app). `bun run harness` también limpio. Sin errores nuevos en consola: el único 400 es el bloque de datos de la página `Inicio`, que ya fallaba antes por declarar una columna que no existe.

## 10. Verificación final

- [x] 10.1 `bun run typecheck`, `bun run lint`, `bun run harness` limpios sobre el repo completo. Los tres limpios, `bun run lint` (`eslint .`) incluido. Para que pasara hubo que añadir `.claude` a los ignorados de `eslint.config.js`: ahí dentro nacen copias enteras del repo (los worktrees de las sesiones) con su propio `tsconfig.json`, el parser ve dos raíces candidatas y se planta con «No tsconfigRootDir was set» en cada archivo de la copia — un worktree olvidado bastaba para que el lint no pasara nunca. Estaba en `.gitignore` pero no en los de ESLint. Sigue revisando los 265 archivos de código real. Los 2 warnings del typecheck son de `PublishPanel.svelte:28` y venían de antes de este cambio.
- [x] 10.2 `bun run dev` + `bun run smoke` pasa completo. 19 bloques, todos en verde hasta «Todo bien».
- [x] 10.3 Revisar que ningún archivo de `AGENTS.md` o `docs/COMO-FUNCIONA.md` quedó con una ruta vieja (`grep` de cada ruta movida contra ambos archivos). Los dos limpios. Se hizo además un barrido más ancho —cada ruta a un archivo que se nombre en cualquier `.md`, `.ts` o `.svelte` del repo, comprobando que el archivo existe— y salieron 12 comentarios que mis movimientos habían dejado apuntando a donde ya no estaba: `server/ai/ai.ts`, `web/src/lib/aiConfigFile.ts`, `shared/icons.ts`, `shared/htmlContract.ts` (dos), `shared/htmlSources.ts`, `server/schema.ts`, `server/access.ts`, `web/src/lib/planeAssets.ts`, `web/src/components/HtmlFrame.svelte`, `docs/MIGRACION-SVELTE.md` y `docs/DOCKER.md`. Corregidos. También se corrigieron dos que no eran de este cambio pero llevaban rotos desde la migración a Svelte: `web/src/lib/tableExport.ts` y `web/src/lib/peopleGrid.ts` nombraban `web/src/components/PersonPassword.tsx`, de la época de React; ahora apuntan a `web/src/components/database/PasswordForm.svelte`, que es quien heredó ese papel (su cabecera dice lo mismo que prometen los dos comentarios: la clave no es una columna, no se guarda y no sale al exportar). Con eso, ninguna ruta nombrada en un `.md`, `.ts` o `.svelte` del repo apunta ya a un archivo que no exista.
